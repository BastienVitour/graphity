import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
	container: {
		flex: 1,
		margin: 10,
		width: "100%",
		textAlign: "center",
		gap: 10
	},
	title: {
		fontWeight: "bold"
	},
	input: {
		borderWidth: 1,
		borderRadius: 5,
		width: "100%",
		borderColor: "gray"
	}
});

export default styles;
